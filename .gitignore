# Session credentials — NEVER commit. These grant full access to a Yahoo account.
browser-profile/
*.cookies
cookies.txt
captures/
.env
.env.*
node_modules/

# PII scrub map for fixture capture — contains real names/GUIDs
tools/scrub-map.local.json
